{
  "name": "meu-conversor",
  "version": "1.0.0",
  "description": "Conversor de moedas com cotação em tempo real",
  "private": true,
  "scripts": {
    "start": "live-server client --host=localhost --port=8080 --no-browser",
    "build": "echo 'Static site, no build required'",
    "dev": "npm start",
    "test": "echo \"No tests specified\" && exit 0"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}